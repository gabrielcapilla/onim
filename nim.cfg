# The embedded nimsuggest API is built from the matching Nim installation.
# `$nim` is resolved by Nim to the active compiler prefix.

path:"$nim"
path:"$nim/lib"
path:"$lib/packages/docutils"
define:onimEmbedded
define:nimcore
define:nimsuggest
gc:orc
threads:on
